generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
}

model Analytics {
  id       String @id @default(uuid()) @map("_id")
  question String
  count    Int    @default(1)
}
